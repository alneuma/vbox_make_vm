# VirtualBox vm setup script

This is a simple script to easily setup a new vm with VirtualBox.
Just modify the variables on top of the script file before you run it.

To get rid of an existing vm use:
`VBoxManage unregistervm <vm-name> --delete`
